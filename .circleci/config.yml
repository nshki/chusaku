version: 2
jobs:
  build:
    working_directory: ~/chusaku
    docker:
      - image: circleci/ruby
    steps:
      - checkout
      - run:
          name: Install Bundler
          command: |
            gem install bundler
      - restore_cache:
          keys:
            - dep-{{ .Branch }}-{{ checksum "chusaku.gemspec" }}
            - dep-{{ .Branch }}
            - dep-
      - run:
          name: Install dependencies
          command: |
            bundle install
      - save_cache:
          key: dep-{{ .Branch }}-{{ chucksum "chusaku.gemspec" }}
          paths:
            - vendor/bundle
            - ~/.bundle
      - run:
          name: Run tests
          command: |
            bundle exec rake test
